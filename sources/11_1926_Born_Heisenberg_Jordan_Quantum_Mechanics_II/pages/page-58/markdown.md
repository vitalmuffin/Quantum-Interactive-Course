614

M. Born, W. Heisenberg und P. Jordan,

Die Matrix $\boldsymbol{q}_k$ hat $2f$ Dimensionen, wenn $f$ die Anzahl der Eigenschwingungen ist; bei der elastischen Saite also unendlich viele. Die Komponenten $q_k(nm)$ von $\boldsymbol{q}_k$ verschwinden sämtlich, außer denen mit

$$\left. \begin{aligned} n_j - m_j &= 0 \text{ für } j \neq k, \\ n_k - m_k &= \pm 1. \end{aligned} \right\} \quad (53)$$

Das Phasenmittel einer Matrix ist diejenige Diagonalmatrix, die mit der Diagonale der betreffenden Matrix übereinstimmt. Aus (53), Kap. 4, können zum Teil ähnliche Folgerungen gezogen werden, wie aus (44), Kap. 4. Die Überlegungen, die früher zu (46), 46'), (46'') führten, bleiben für die Quantenmechanik erhalten. Auch gelten für die Diagonalmatrix $\overline{\boldsymbol{\Delta}_1^2 + \boldsymbol{\Delta}_2^2}$ die Formeln (47), (47'), Kap. 4, mit Matrizen $\boldsymbol{q}_k$ und endlich wird entsprechend (52), Kap. 4, wenn wir die zu einem bestimmten $\boldsymbol{v}$ gehörigen Teile von $\overline{\boldsymbol{\Delta}}^2$ als $\overline{\boldsymbol{\Delta}}^2$ bezeichnen:

$$\overline{\boldsymbol{\Delta}_1^2 + \boldsymbol{\Delta}_2^2} = \frac{\overline{E}^{*2}}{2v}. \quad (52')$$

Darin ist gemäß (49), (50), (51), Kap. 4, $E^*$ nicht mehr die mittlere thermische Energie, sondern die Summe von dieser und [der Nullpunktsenergie; nach den elementaren Oszillatorformeln ist

$$\overline{E}^* = h\boldsymbol{v}\cdot\boldsymbol{v} + \overline{E},$$

$$\overline{\boldsymbol{\Delta}_1^2 + \boldsymbol{\Delta}_2^2} = \frac{1}{2}(h\boldsymbol{v})^2\boldsymbol{v} + h\boldsymbol{v}\overline{E} + \frac{\overline{E}^2}{2v}; \quad (54)$$

denn die Nullpunktsenergie für $d\boldsymbol{v}$ wird gleich

$$\frac{v}{l} \cdot \frac{h\boldsymbol{v}}{2} \cdot l z_v d\boldsymbol{v} = h\boldsymbol{v} \cdot V d\boldsymbol{v}.$$

Wir haben nun noch $\boldsymbol{\Delta}_1\boldsymbol{\Delta}_2 + \boldsymbol{\Delta}_2\boldsymbol{\Delta}_1$ zu betrachten. Indem wir diese Größe ganz entsprechend behandeln wie $\overline{\boldsymbol{\Delta}_1^2 + \boldsymbol{\Delta}_2^2}$, erhalten wir entsprechend zu (49), Kap. 4, den Ausdruck

$$\overline{\boldsymbol{\Delta}_1\boldsymbol{\Delta}_2 + \boldsymbol{\Delta}_2\boldsymbol{\Delta}_1} = \frac{a l^2}{8\pi} \int_0^\infty d\boldsymbol{\omega} \cdot \boldsymbol{\omega}^2 \left\{ (\boldsymbol{q}_\omega \dot{\boldsymbol{q}}_\omega)^2 + (\dot{\boldsymbol{q}}_\omega \boldsymbol{q}_\omega)^2 \right\}.$$

Nach den Vertauschungsregeln wird nun aber, da zufolge (42), Kap. 4, die Größe $l/2$ als „Masse“ der Resonatoren anzusehen ist:

$$-\boldsymbol{q}_j \dot{\boldsymbol{q}}_j(nn) = \dot{\boldsymbol{q}}_j \boldsymbol{q}_j(nn) = \frac{1}{2} \cdot \frac{2}{l} \cdot \frac{h}{2\pi i} = \frac{h}{2l\pi i}.$$

Folglich wird der zu $d\boldsymbol{v}$ gehörige Anteil $\overline{\boldsymbol{\Delta}_1\boldsymbol{\Delta}_2 + \boldsymbol{\Delta}_2\boldsymbol{\Delta}_1}$ von $\overline{\boldsymbol{\Delta}_1\boldsymbol{\Delta}_2 + \boldsymbol{\Delta}_2\boldsymbol{\Delta}_1}$ nach Division durch $d\boldsymbol{v}$ gleich

$$\overline{\boldsymbol{\Delta}_1\boldsymbol{\Delta}_2 + \boldsymbol{\Delta}_2\boldsymbol{\Delta}_1} = -\frac{1}{2}(h\boldsymbol{v})^2 V,$$