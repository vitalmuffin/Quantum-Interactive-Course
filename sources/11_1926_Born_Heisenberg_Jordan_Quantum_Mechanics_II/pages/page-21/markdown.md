Zur Quantenmechanik. II.

577

und $\boldsymbol{E}_{\text{pot}}$ eine homogene Funktion der Koordinaten vom Grade $n$. Es wird dann nach (3), Kap. 2:

$$\boldsymbol{E}_{\text{pot}} = \frac{1}{n} \sum_k \frac{\partial \boldsymbol{E}_{\text{pot}}}{\partial \boldsymbol{q}_k} \boldsymbol{q}_k \quad (16)$$

und

$$\frac{d}{dt} \sum_k \boldsymbol{p}_k \boldsymbol{q}_k = \sum_k (\dot{\boldsymbol{p}}_k \boldsymbol{q}_k + \boldsymbol{p}_k \dot{\boldsymbol{q}}_k) = 2 \boldsymbol{E}_{\text{kin}} - n \boldsymbol{E}_{\text{pot}},$$

also für die Mittelwerte

$$\overline{\boldsymbol{E}}_{\text{kin}} = \frac{n}{2} \overline{\boldsymbol{E}}_{\text{pot}}. \quad (17)$$

Z. B. wird für $n = 2$ (harmonische Schwingungen) $\overline{\boldsymbol{E}}_{\text{kin}} = \overline{\boldsymbol{E}}_{\text{pot}}$ und für $n = -1$ (Coulombsche Kräfte) $\overline{\boldsymbol{E}}_{\text{kin}} = -\frac{1}{2} \overline{\boldsymbol{E}}_{\text{pot}}$.

§ 2. Entartete Systeme. Wir wollen nun die entarteten Systeme ins Auge fassen. Wird das Verschwinden einiger der Frequenzen $\boldsymbol{v}(nm)$ zugelassen (der Einfachheit halber denken wir uns die Matrizen zweidimensional dargestellt), so kann die Energiekonstanz $\dot{\boldsymbol{H}} = 0$ noch immer durch die in I und hier ausgeführten Überlegungen aus den Bewegungsgleichungen und den Vertauschungsregeln (3), Kap. 2, abgeleitet werden. Aber $\dot{\boldsymbol{H}} = 0$ hat nicht mehr notwendig die Folge, daß $\boldsymbol{H}$ eine Diagonalmatrix ist, und der Beweis des Frequenzsatzes wird damit undurchführbar. Es sind also bei entarteten Systemen die Bewegungsgleichungen zusammen mit (3), Kap. 2, allein nicht ausreichend zur eindeutigen Festlegung der Eigenschaften des Systems, und es ist eine Verschärfung dieser Grundgleichungen nötig. Es liegt nahe anzunehmen, daß diese Verschärfung so zu fassen ist: Als Grundgleichungen sollen allgemein gewählt werden können die Vertauschungsrelationen und

$$\boldsymbol{H} = \boldsymbol{W} = \text{Diagonalmatrix}. \quad (18)$$

Durch diese Forderung ist offenbar die Gültigkeit der Frequenzbedingung auch für die entarteten Systeme gesichert. Sehr wahrscheinlich wird dabei auch (von singulären Fällen abgesehen) die Energie $\boldsymbol{W}$ stets eindeutig bestimmt sein. Dagegen werden die Koordinaten $\boldsymbol{q}_k$ nicht eindeutig festgelegt. Man kann sich, wenn man eine Lösung $\boldsymbol{p}_k, \boldsymbol{q}_k$ von $\boldsymbol{H}(\boldsymbol{p}\,\boldsymbol{q}) = \boldsymbol{W}$ besitzt, neue Lösungen verschaffen durch den Ansatz

$$\left. \begin{array}{l} \boldsymbol{p}' = \boldsymbol{S} \boldsymbol{p} \boldsymbol{S}^{-1}, \\ \boldsymbol{q}' = \boldsymbol{S} \boldsymbol{q} \boldsymbol{S}^{-1}. \end{array} \right\} \quad (19)$$

Damit wird

$$\boldsymbol{H}(\boldsymbol{p}'\,\boldsymbol{q}') = \boldsymbol{W}' = \boldsymbol{S} \boldsymbol{W} \boldsymbol{S}^{-1},$$